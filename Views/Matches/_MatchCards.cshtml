@model List<BoardGameLeague.Models.Match>

<div class="table-responsive custom-table-wrapper">
    <table class="table custom-table">
        <thead>
            <tr>
                <th>Match</th>
                <th>Tournament</th>
                <th>Game</th>
                <th>Start</th>
                <th>Score</th>
                <th>Details</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var match in Model)
            {
                <tr>
                    <td>@match.TeamA.Name vs @match.TeamB.Name</td>
                    <td><a asp-controller="Tournaments" asp-action="Details" asp-route-id="@match.Tournament.Id">@match.Tournament.Name</a></td>
                    <td><a asp-controller="BoardGames" asp-action="Details" asp-route-id="@match.Game.Id">@match.Game.Name</a></td>
                    <td>@(match.StartTime.ToString("yyyy-MM-dd HH:mm"))</td>
                    <td>@match.ScoreA - @match.ScoreB</td>
                    <td>
                        <a class="btn btn-sm btn-outline-info" asp-action="Details" asp-route-id="@match.Id">Open</a>
                        @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
                        {
                            <a class="btn btn-sm btn-outline-secondary ms-2" asp-action="Edit" asp-route-id="@match.Id">Edit</a>
                            <a class="btn btn-sm btn-outline-danger ms-2 btn-confirm-delete" data-entity="match" asp-action="Delete" asp-route-id="@match.Id">Delete</a>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
